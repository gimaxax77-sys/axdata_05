<!-- 오후 10시 PC 작업 체크리스트 -->
# 🌙 오후 10시 PC 작업 체크리스트 (2026-07-14)

> 미니PC 작업. 위에서 아래로. 결과는 `research.md`에 기록.
> 흐름: ① 33명 이미지 자동생성 → ② 검수 → ③ 3D 변환(승인분만) → ④ 2D 렌더

## 0. 최신 도구 받기
- [ ] `update.bat` 더블클릭 (오늘 만든 자동생성 도구까지 최신화)

## 1. 33명 이미지 자동 생성 ⭐ (핵심 · 무료)
- [ ] `gen-pollinations.bat` 더블클릭
      → (파이썬 없으면 안내대로 설치 후 재실행)
      → `out_gen/` 에 `kael.png … nocturne.png` 33장 자동 저장
- [ ] 33장 훑어보고 **검수 게이트** 확인
      (얼굴 대칭 · 손 · 큰머리 치비 비율 통일 · 무기없음 · 흰배경)
- [ ] 마음에 안 드는 캐릭터만 → `scripts/gen_pollinations.py` 의 `CHARS` 설명 수정
      + `out_gen/<id>.png` 삭제 후 `gen-pollinations.bat` 재실행(그 캐릭만 다시)

## 2. 3D 변환 (승인된 것만 · Tripo 무료 12회 남음)
- [ ] 통과 이미지를 Tripo `이미지 → 3D(HD 모델)` 변환 → GLB 내보내기(왼쪽 '보내기')
- [ ] 우선 대표 몇 명(카엘 등)만 먼저 → 파이프라인 재확인
- [ ] GLB 를 `input/` 폴더에 넣기

## 3. 2D 렌더 (스프라이트)
- [ ] `render-folder.bat` → `[2] Tripo 정면 Xm` (또는 `[1] 4방향`)
- [ ] `out/` 결과 확인 → 게임 크기로 줄여도 구분되는지

## 4. 마무리
- [ ] 승인 이미지/렌더를 `reference/` 또는 게임팩 규격 `assets/char/<concept>/<id>.png` 로 배치(선택)
- [ ] 오늘 진행·결과를 `research.md` 에 기록

---
### 참고
- 이 저장소: `README.md` · `MESHY.md`(AI 생성 파이프라인)
- 아티팩트(프롬프트·규칙·품질게이트): 대화의 '캐릭터 파이프라인' 카드
- 기준작 이미지: `reference/` (kael·luna·gwen·ciel)
